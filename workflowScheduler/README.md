# Workflow Scheduler

The Workflow Scheduler requires the profiling data in two CSV files. One for the file-transfers the other for the functions. 

First use the `preprocessor.py` to generate the required CSV files.

Upon completion use the `scheduler.py` to schedule the workflow and generated a updated `workflowConfig.json` file.