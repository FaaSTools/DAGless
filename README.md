# DAGless

DAGless is modular pipeline of four components, each of which is contained in its own directory. The components are:

1. `worklfowAgnosticFaaSifier`
2. `deployer`
3. `fileInvestigationAndReplicationExecutionEngine`
4. `workflowScheduler`

Additionally there is the source code and executables for the `directorMonitor`.
