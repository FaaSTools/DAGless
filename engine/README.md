# Invoker

The invoker is the component that is responsible for executing the workflow thereby learning the data flow and profiling the functions.

## Usage

Ensure that the dependencies defined in `Pipefile` are installed.

Next set the variables in the `invoker.py` file to the appropriate values.

Finally run the invoker using the command `python3 invoker.py`.
